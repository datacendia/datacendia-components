# =============================================================================
# DATACENDIA LOCAL FALCO RULES - Custom Overrides
# =============================================================================
# Add local rule customizations here without modifying the main rules file

# Example: Disable a noisy rule
# - rule: Terminal shell in container
#   enabled: false

# Example: Override priority
# - rule: Sensitive file access in container
#   priority: NOTICE
