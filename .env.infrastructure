# =============================================================================
# DATACENDIA INFRASTRUCTURE ENVIRONMENT VARIABLES
# =============================================================================
# Copy this to .env and update with your actual values

# Redis Configuration
REDIS_URL=redis://:datacendia2024@localhost:6380
REDIS_PASSWORD=datacendia2024

# Neo4j Configuration
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=datacendia2024

# Apache Druid Configuration (currently restarting - optional)
DRUID_URL=http://localhost:8888
DRUID_BROKER_URL=http://localhost:8082

# ClickHouse Configuration
CLICKHOUSE_URL=http://localhost:8123
CLICKHOUSE_USER=datacendia
CLICKHOUSE_PASSWORD=datacendia2024
CLICKHOUSE_DATABASE=datacendia

# Keycloak Configuration (currently restarting - optional)
KEYCLOAK_URL=http://localhost:8180
KEYCLOAK_REALM=datacendia
KEYCLOAK_CLIENT_ID=datacendia-backend
KEYCLOAK_CLIENT_SECRET=your-client-secret-here

# Apache Tika Configuration
TIKA_URL=http://localhost:9998

# Grafana Configuration
GRAFANA_URL=http://localhost:3100
GRAFANA_USER=admin
GRAFANA_PASSWORD=datacendia2024

# Tempo Configuration (currently restarting - optional)
TEMPO_URL=http://localhost:3200
TEMPO_OTLP_GRPC=http://localhost:4317
TEMPO_OTLP_HTTP=http://localhost:4318

# Prometheus Configuration
PROMETHEUS_URL=http://localhost:9090
